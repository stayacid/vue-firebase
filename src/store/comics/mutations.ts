import { MutationTree } from "vuex";
import { IComicsState } from "./types";

export const mutations: MutationTree<IComicsState> = {
  /*addItem(state, data: StandardCartItem) {
    state.items.push(data);
  },*/
}
